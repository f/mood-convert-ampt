export default {
  // roots: ['<rootDir>'],
  roots: ['<rootDir>'],
  moduleNameMapper: {
    '@ampt/sdk/events': '<rootDir>/../sdk/events/index.ts'
  },
  transform: {
    '\\.ts$': [
      'esbuild-jest',
      {
        target: `node${process.version.slice(1)}`,
        sourcemap: 'inline',
        sourcesContent: true
      }
    ]
  }
}
